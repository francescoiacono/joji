{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"CLIENT,SERVER\" -c \"bgBlack.bold,bgMagenta.bold\" \"npm run dev --workspace=packages/frontend\" \"npm run dev --workspace=packages/backend\"",
    "prepare": "husky install",
    "build:types": "tsc -p packages/types"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "husky": "^8.0.3"
  }
}
