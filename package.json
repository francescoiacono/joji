{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"CLIENT, SERVER\" -c \"bgWhite.bold,bgMagenta.bold\" \"npm run dev --workspace=packages/frontend\" \"npm run dev --workspace=packages/backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
